---
 - name: install nginx
   hosts: all
   become: yes
   tasks: 
     - name: install nginx
       yum:
         name: nginx
         state: present
     - name: start service 
       service:
         name: nginx
         state: started
         enabled: yes
