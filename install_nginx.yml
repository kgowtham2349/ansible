---
 -name: install nginx
 hosts: all
 become: yes
 tasks:
  -name: install nginx
   yum:
    name: nginx
    state: present
    enabled: yes
  -name: start the service
   service:
    name: nginx
    state: started

 